package views

import "din-invoice/models"
import "fmt"

templ InvoiceView(inv *models.Invoice, settings models.AppSettings) {
	@Layout("Rechnung " + inv.InvoiceNumber) {
		<div class="no-print mb-8 flex justify-between items-center">
			<a href="/" class="text-blue-600 hover:underline flex items-center gap-1">
				<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
					<path stroke-linecap="round" stroke-linejoin="round" d="M10.5 19.5 3 12m0 0 7.5-7.5M3 12h18"></path>
				</svg>
				Zurück zur Übersicht
			</a>
			<div class="flex gap-2">
				<a href={ templ.SafeURL("/invoices/" + fmt.Sprintf("%d", inv.ID) + "/edit") } class="bg-gray-200 hover:bg-gray-300 text-gray-800 font-bold py-2 px-4 rounded flex items-center gap-2">
					<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
						<path stroke-linecap="round" stroke-linejoin="round" d="m16.862 4.487 1.687-1.688a1.875 1.875 0 1 1 2.652 2.652L10.582 16.07a4.5 4.5 0 0 1-1.897 1.13L6 18l.8-2.685a4.5 4.5 0 0 1 1.13-1.897l8.932-8.931Zm0 0L19.5 7.125M18 14v4.75A2.25 2.25 0 0 1 15.75 21H5.25A2.25 2.25 0 0 1 3 18.75V8.25A2.25 2.25 0 0 1 5.25 6H10"></path>
					</svg>
					Bearbeiten
				</a>
				<a href={ templ.SafeURL("/invoices/" + fmt.Sprintf("%d", inv.ID) + "/pdf") } target="_blank" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded flex items-center gap-2">
					<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-4 h-4">
						<path stroke-linecap="round" stroke-linejoin="round" d="M6.72 13.829c-.24.03-.48.062-.72.096m.72-.096a42.415 42.415 0 0 1 10.56 0m-10.56 0L6.34 18m10.94-4.171c.24.03.48.062.72.096m-.72-.096L17.66 18m0 0 .229 2.523a1.125 1.125 0 0 1-1.12 1.227H7.231c-.662 0-1.18-.568-1.12-1.227L6.34 18m11.318 0h1.091A2.25 2.25 0 0 0 21 15.75V9.456c0-1.081-.768-2.015-1.837-2.175a48.055 48.055 0 0 0-1.913-.247M6.34 18H5.25A2.25 2.25 0 0 1 3 15.75V9.456c0-1.081.768-2.015 1.837-2.175a48.041 48.041 0 0 1 1.913-.247m10.5 0a48.536 48.536 0 0 0-10.5 0m10.5 0V3.375c0-.621-.504-1.125-1.125-1.125h-8.25c-.621 0-1.125.504-1.125 1.125v3.659M18 10.5h.008v.008H18V10.5Zm-3 0h.008v.008H15V10.5Z"></path>
					</svg>
					PDF Laden
				</a>
				<a href={ templ.SafeURL("/invoices/" + fmt.Sprintf("%d", inv.ID) + "/pdf?force=1") } target="_blank" class="bg-gray-100 hover:bg-gray-200 text-gray-600 py-2 px-3 rounded text-xs flex items-center gap-1 border border-gray-300" title="PDF bei Bedarf manuell neu generieren">
					<svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor" class="w-3 h-3">
						<path stroke-linecap="round" stroke-linejoin="round" d="M16.023 9.348h4.992v-.001M2.985 19.644v-4.992m0 0h4.992m-4.993 0 3.181 3.183a8.25 8.25 0 0 0 13.803-3.7M4.031 9.865a8.25 8.25 0 0 1 13.803-3.7l3.181 3.182m0-4.991v4.99"></path>
					</svg>
					PDF neu erzeugen
				</a>
			</div>
		</div>

		if inv.InternalNote != "" || inv.PaymentDetails != "" {
			<div class="no-print bg-yellow-50 border-l-4 border-yellow-400 p-4 mb-8 text-yellow-800 rounded shadow-sm">
				if inv.InternalNote != "" {
					<div class="mb-2">
						<p class="font-bold text-sm mb-1 uppercase tracking-wide">Interne Notiz (Wird nicht gedruckt)</p>
						<p class="whitespace-pre-line text-sm">{ inv.InternalNote }</p>
					</div>
				}
				if inv.PaymentDetails != "" {
					<div>
						<p class="font-bold text-sm mb-1 uppercase tracking-wide">Zahlungsdetails (Intern)</p>
						<p class="whitespace-pre-line text-sm">{ inv.PaymentDetails }</p>
					</div>
				}
			</div>
		}

		<div class="page text-sm text-black relative font-serif z-10">
			if inv.Status == "Entwurf" {
				<div class="absolute inset-0 flex items-center justify-center opacity-10 pointer-events-none z-0 overflow-hidden">
					<div class="transform -rotate-45 text-[100pt] font-bold text-gray-500 border-8 border-gray-500 p-10 rounded">ENTWURF</div>
				</div>
			} else if inv.Status == "Bezahlt" {
				<div class="absolute inset-0 flex items-center justify-center opacity-20 pointer-events-none z-0 overflow-hidden">
					<div class="transform -rotate-45 text-[100pt] font-bold text-green-600 border-8 border-green-600 p-10 rounded">BEZAHLT</div>
				</div>
			} else if inv.Status == "Storniert" {
				<div class="absolute inset-0 flex items-center justify-center opacity-20 pointer-events-none z-0 overflow-hidden">
					<div class="transform -rotate-45 text-[80pt] font-bold text-red-600 border-8 border-red-600 p-10 rounded">STORNIERT</div>
				</div>
			}
			<!-- Address Field -->
			<div class="address-field z-10">
				<div class="sender-line text-gray-600 truncate">
					{ inv.SenderName } • { inv.SenderAddress }
				</div>
				<div class="mt-4">
					<div class="font-bold text-lg mb-1">{ inv.RecipientName }</div>
					<div class="whitespace-pre-line leading-snug">{ inv.RecipientAddress }</div>
				</div>
			</div>
			<!-- Info Block -->
			<div class="info-block text-right z-10">
				<h2 class="text-xl font-bold mb-4">RECHNUNG</h2>
				<table class="ml-auto text-sm">
					<tr>
						<td class="font-bold pr-4 py-1 text-gray-600">Rechnungs-Nr.</td>
						<td class="py-1">{ inv.InvoiceNumber }</td>
					</tr>
					<tr>
						<td class="font-bold pr-4 py-1 text-gray-600">Datum</td>
						<td class="py-1">{ inv.Date }</td>
					</tr>
					if inv.CustomerID != nil {
						<tr>
							<td class="font-bold pr-4 py-1 text-gray-600">Kundennummer</td>
							<td class="py-1">{ inv.CustomerNumber }</td>
						</tr>
					}
				</table>
			</div>
			<!-- Content Area -->
			<div class="mt-[90mm] relative z-10">
				<!-- Push content down below address/header -->
				<p class="mb-4">Sehr geehrte Damen und Herren,</p>
				<p class="mb-8">vielen Dank für Ihren Auftrag. Wir stellen Ihnen folgende Leistungen in Rechnung:</p>
				<table class="w-full mb-8 border-collapse">
					<thead>
						<tr class="border-b-2 border-black text-xs uppercase tracking-wider">
							<th class="text-left py-2 font-bold w-12">Pos.</th>
							<th class="text-left py-2 font-bold">Beschreibung</th>
							<th class="text-right py-2 font-bold w-24">Menge</th>
							<th class="text-right py-2 font-bold w-32">Einzelpreis</th>
							<th class="text-right py-2 font-bold w-32">Gesamt</th>
						</tr>
					</thead>
					<tbody>
						for i, item := range inv.Items {
							<tr class="border-b border-gray-200">
								<td class="py-3 valign-top">{ fmt.Sprintf("%d", i+1) }</td>
								<td class="py-3 valign-top">{ item.Description }</td>
								<td class="py-3 text-right valign-top">{ fmt.Sprintf("%d", item.Quantity) }</td>
								<td class="py-3 text-right valign-top">{ models.FormatCurrency(item.PricePerUnit) }</td>
								<td class="py-3 text-right valign-top font-medium">{ models.FormatCurrency(float64(item.Quantity) * item.PricePerUnit) }</td>
							</tr>
						}
					</tbody>
				</table>
				<div class="flex justify-end mt-8">
					<table class="w-1/2">
						<tr>
							<td class="text-right py-1 text-gray-600">Nettobetrag:</td>
							<td class="text-right py-1 font-mono w-32">{ models.FormatCurrency(inv.TotalNet()) }</td>
						</tr>
						if !inv.IsSmallBusiness {
							<tr>
								<td class="text-right py-1 text-gray-600">Umsatzsteuer ({ fmt.Sprintf("%.0f", inv.TaxRate) }%):</td>
								<td class="text-right py-1 font-mono">{ models.FormatCurrency(inv.TaxAmount()) }</td>
							</tr>
						}
						<tr class="text-lg">
							<td class="text-right py-3 font-bold border-t-2 border-black">Gesamtbetrag:</td>
							<td class="text-right py-3 font-bold font-mono border-t-2 border-black">{ models.FormatCurrency(inv.TotalGross()) }</td>
						</tr>
					</table>
				</div>
				if inv.IsSmallBusiness {
					<p class="mt-4 text-xs text-gray-600 italic">
						Gemäß § 19 UStG wird keine Umsatzsteuer berechnet.
					</p>
				}
				if inv.DocumentNote != "" {
					<div class="mt-6 mb-2 whitespace-pre-line text-sm text-gray-800">
						{ inv.DocumentNote }
					</div>
				}
				<p class="mt-8 text-sm text-gray-700">Bitte überweisen Sie den Gesamtbetrag innerhalb von 14 Tagen auf das unten genannte Konto unter Angabe der Rechnungsnummer.</p>
			</div>
			<!-- Footer -->
			<div class="absolute bottom-[20mm] left-[20mm] right-[20mm] text-[8pt] text-gray-500 border-t border-gray-300 pt-4 z-10">
				<div class="grid grid-cols-3 gap-4">
					<div>
						<div class="font-bold text-gray-700 mb-1">{ settings.SenderName }</div>
						<div class="whitespace-pre-line">{ settings.SenderAddress }</div>
					</div>
					<div>
						<div class="font-bold text-gray-700 mb-1">Bankverbindung</div>
						<div>Bank: { settings.BankName }</div>
						<div>IBAN: { settings.IBAN }</div>
						<div>BIC: { settings.BIC }</div>
					</div>
					<div>
						<div class="font-bold text-gray-700 mb-1">Kontakt</div>
						<div>Web: { settings.Website }</div>
						<div>Email: { settings.Email }</div>
					</div>
				</div>
			</div>
		</div>
	}
}
